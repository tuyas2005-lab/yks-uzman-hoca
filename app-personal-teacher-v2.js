(()=>{
  const root=document.getElementById('teacher');if(!root)return;
  window.__teacherPolicyPending=true;
  const D=()=>window.YKSDataV5,C=()=>window.YKSQuestionCatalogV1;
  const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#039;'}[c]));
  const norm=s=>D()?.norm?.(s)||String(s||'').toLocaleLowerCase('tr-TR').replace(/[^a-z0-9çğıöşü]+/g,' ').trim();
  const today=()=>D()?.todayKey?.()||new Date().toLocaleDateString('sv-SE');
  state.teacher??={};state.teacher.recapCache??={};state.miniTests??={history:[]};

  const css=document.createElement('style');css.textContent=`
  #teacher .pt-intro,#teacher .pt-focus,#teacher>.section-title,#teacher>.pt-flow,#teacher>#ptLessonSlot,#teacher>.dash-grid.pt-section{display:none!important}
  .pt2{display:grid;gap:14px}.pt2-hero{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:18px;align-items:center;padding:20px;border:1px solid #ddd7ff;border-radius:21px;background:linear-gradient(135deg,#f5f2ff,#fff)}.pt2-chips{display:flex;gap:7px;flex-wrap:wrap}.pt2-chip{font-size:11px;font-weight:850;padding:5px 8px;border-radius:999px;background:#ece7ff;color:#5a43d1}.pt2-chip.green{background:#eaf7ef;color:#197249}.pt2-hero h2{font-size:28px;margin:8px 0 5px}.pt2-guidance{margin-top:12px;padding:13px 15px;border-left:4px solid #6849ed;border-radius:0 14px 14px 0;background:rgba(255,255,255,.78);box-shadow:0 4px 14px rgba(66,48,150,.06)}.pt2-guidance-label{display:block;color:#6047d5;font-size:10px;font-weight:900;letter-spacing:.08em;text-transform:uppercase}.pt2-guidance p{max-width:820px;margin:5px 0 0;color:var(--ink);font-size:15px;font-weight:650;line-height:1.62}.pt2-progress{text-align:center;min-width:118px}.pt2-progress b{display:block;font-size:31px}.pt2-progress small{color:var(--muted)}.pt2-bar{height:8px;background:#ebe8f4;border-radius:999px;overflow:hidden;margin-top:9px}.pt2-bar i{display:block;height:100%;background:linear-gradient(90deg,#6849ed,#5368ff)}
  .pt2-main{display:grid;grid-template-columns:minmax(0,1.45fr) minmax(260px,.55fr);gap:14px}.pt2-card{background:var(--surface);border:1px solid var(--line);border-radius:19px;padding:17px}.pt2-card h3{margin:0}.pt2-head{display:flex;justify-content:space-between;align-items:center;gap:10px;margin-bottom:12px}.pt2-head small{color:var(--muted)}
  .pt2-tasks{display:grid;gap:9px}.pt2-task{display:grid;grid-template-columns:42px minmax(0,1fr) auto;gap:12px;align-items:center;border:1px solid var(--line);border-radius:15px;padding:13px;background:var(--surface)}.pt2-task.active{border-color:#a99bf7;background:#faf9ff;box-shadow:0 5px 18px rgba(92,69,210,.07)}.pt2-task.done{border-color:#bfe5ce;background:#f7fcf9}.pt2-task.locked{opacity:.58}.pt2-num{width:36px;height:36px;border-radius:11px;display:grid;place-items:center;font-weight:900;background:#eeeaff;color:#5942d1}.pt2-task.done .pt2-num{background:#dcf4e6;color:#187649}.pt2-task h4{margin:0 0 3px;font-size:15px}.pt2-task p{margin:0;color:var(--muted);font-size:12px;line-height:1.4}.pt2-status{font-size:10px;font-weight:850;padding:5px 8px;border-radius:999px;background:#f1eff8;color:#6b6480;white-space:nowrap}.pt2-task.active .pt2-status{background:#eee9ff;color:#5a43d1}.pt2-task.done .pt2-status{background:#e4f6eb;color:#187649}.pt2-task button{min-width:132px}
  .pt2-metrics{display:grid;grid-template-columns:1fr 1fr;gap:9px}.pt2-metric{border:1px solid var(--line);border-radius:14px;padding:13px;text-align:center}.pt2-metric b{display:block;font-size:21px}.pt2-metric small{color:var(--muted);font-size:11px}.pt2-note{margin-top:10px;padding:11px;border-radius:13px;background:#f7f6fc;color:var(--muted);font-size:12px;line-height:1.45}
  .pt2-next{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:18px;align-items:center;margin-top:14px;padding:20px;border:1px solid #b9dfc7;border-radius:20px;background:linear-gradient(135deg,#f0fbf4,#f7f6ff);box-shadow:0 12px 30px rgba(32,118,75,.10)}.pt2-next-copy{display:flex;gap:13px;align-items:center}.pt2-next-icon{width:48px;height:48px;flex:0 0 48px;border-radius:15px;display:grid;place-items:center;background:#dff5e7;color:#187649;font-size:24px;font-weight:950}.pt2-next h3{margin:0 0 5px;font-size:19px}.pt2-next p{margin:0;color:var(--muted);font-size:13px;line-height:1.5}.pt2-next button{min-width:260px;min-height:54px;border:1px solid #2946ca!important;border-radius:14px!important;background:linear-gradient(180deg,#6b7cff,#4055e9 58%,#3146c7)!important;color:#fff!important;font-size:16px;font-weight:950;box-shadow:0 6px 0 #2638a7,0 11px 22px rgba(42,58,169,.22)!important;transition:transform .1s ease,box-shadow .1s ease}.pt2-next button:active{transform:translateY(5px);box-shadow:0 1px 0 #2638a7,0 5px 11px rgba(42,58,169,.18)!important}
  .pt2-recap{padding:17px;border:1px solid #d9e3ff;border-radius:17px;background:#f7f9ff}.pt2-recap h3{margin:0 0 5px}.pt2-key{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin:12px 0}.pt2-key div{padding:11px;border:1px solid #e3e7f6;border-radius:13px;background:#fff;line-height:1.4;font-size:13px}.pt2-warn{padding:11px;border-radius:13px;background:#fff6e9;border:1px solid #f3dfba;margin-bottom:9px}.pt2-tip{padding:11px;border-radius:13px;background:#eef8f3;border:1px solid #cfe7d9}.pt2-recap-actions{display:flex;gap:8px;margin-top:12px;flex-wrap:wrap}
  #teacher .back{width:44px;height:44px;flex:0 0 44px}.pt2-details{display:grid;grid-template-columns:1fr 1fr;gap:14px}.pt2-details details{background:var(--surface);border:1px solid var(--line);border-radius:17px;padding:14px}.pt2-details>details>summary{cursor:pointer;font-weight:850;min-height:44px;display:flex;align-items:center}.pt2-health-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;margin-top:11px}.pt2-topics{display:grid;gap:7px;margin-top:11px}.pt2-topic{display:flex;justify-content:space-between;gap:10px;align-items:center;border:1px solid var(--line);border-radius:12px;background:var(--surface);padding:10px;min-height:44px;text-align:left}.pt2-topic.active{border-color:#a99bf7;background:#f8f6ff}.pt2-perfs{display:flex;gap:6px;flex-wrap:wrap;margin-top:11px}.pt2-perf{font-size:11px;padding:6px 8px;border-radius:10px;background:#f2f2f7}.pt2-perf.ok{background:#eaf7ef;color:#197249}.pt2-perf.bad{background:#fff0f1;color:#b73845}.teacher-return{margin:10px 0 12px;padding:11px 13px;border:1px solid #ddd7ff;background:#f7f5ff;border-radius:14px;display:flex;justify-content:space-between;gap:10px;align-items:center}
  .pt2-audits{display:grid;gap:10px;margin-top:12px}.pt2-audit{border:1px solid var(--line);border-radius:15px;padding:13px;background:#fbfbfe}.pt2-audit-head{display:flex;align-items:flex-start;justify-content:space-between;gap:10px}.pt2-audit-head small{display:block;color:var(--muted);margin-top:2px}.pt2-audit-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin-top:10px}.pt2-audit-grid>div{display:grid;gap:3px;padding:10px;border-radius:12px;background:#fff;border:1px solid #eceaf4}.pt2-audit-grid small,.pt2-audit-grid span{color:var(--muted);font-size:11px}.pt2-audit p{margin:9px 0 0;color:var(--muted);font-size:12px}.pt2-audit-ids{margin-top:9px!important;padding:0!important;border:0!important;background:transparent!important}.pt2-audit-ids summary{font-size:11px}.pt2-audit-ids code{display:block;margin-top:7px;white-space:normal;overflow-wrap:anywhere;font-size:10px;color:var(--muted)}
  @media(max-width:900px){.pt2-main,.pt2-details{grid-template-columns:1fr}.pt2-hero{grid-template-columns:1fr}.pt2-progress{text-align:left}.pt2-task{grid-template-columns:40px minmax(0,1fr)}.pt2-task>div:last-child{grid-column:1/-1;display:flex;align-items:center;justify-content:space-between;gap:8px}.pt2-task>div:last-child button{width:auto;min-height:44px;flex:1;margin-left:0!important}.pt2-next{grid-template-columns:1fr}.pt2-next button{width:100%;min-width:0}.pt2-key{grid-template-columns:1fr}}
  @media(max-width:600px){.pt2-hero,.pt2-card{padding:14px}.pt2-hero h2{font-size:24px}.pt2-head{align-items:flex-start;flex-direction:column}.pt2-task>div:last-child{align-items:stretch;flex-direction:column}.pt2-task>div:last-child button{width:100%}.pt2-metrics{grid-template-columns:1fr 1fr}.pt2-audit-grid,.pt2-health-grid{grid-template-columns:1fr}.pt2-audit-head{flex-direction:column}}`;
  document.head.appendChild(css);

  function candidates(){const s=window.getStudentStrategy?.();if(s?.topics?.length){const list=(s.focus?[s.focus.primary,...(s.focus.alternatives||[])]:s.topics).filter(Boolean),seen=new Set();return list.filter(x=>{const k=`${x.exam}|${x.subject}|${x.topic}`;if(seen.has(k))return false;seen.add(k);return true}).slice(0,8)}const m=D()?.getLearningModel?.();return (m?.topics||[]).map(x=>({...x,priority:(100-(x.score??70))+(x.recentWrong||0)*8+(x.recentSignals||0)*10})).sort((a,b)=>b.priority-a.priority)}
  function focus(){const list=candidates(),sel=state.teacher.selectedTopic;if(sel){const hit=list.find(x=>norm(x.topic)===norm(sel));if(hit)return hit}return list[0]||{exam:'TYT',subject:'Matematik',topic:'Genel tekrar',score:null,total:0,wrong:0,recentWrong:0,recentSignals:0,confidence:'Veri yok'}}
  function ensureDaily(f){let d=state.teacher.daily;if(!d||d.date!==today()||norm(d.topic)!==norm(f.topic)){d={date:today(),topic:f.topic,testDone:false,recapDone:false,wrongDone:false,testSummary:null};state.teacher.daily=d;save()}if(d.testDone==null)d.testDone=false;if(d.recapDone==null)d.recapDone=false;if(d.wrongDone==null)d.wrongDone=false;return d}
  const reviewsToday=()=> (state.studyEvents||[]).filter(x=>x?.source==='wrong-review'&&x.dateKey===today()).length;
  function syncWrong(d){const mistakes=Number(d.testSummary?.mistakes||0);if(d.testDone&&d.testSummary&&mistakes===0)d.wrongDone=true;if(d.testDone&&mistakes>0&&reviewsToday()>Number(d.wrongReviewBaseline||0))d.wrongDone=true}
  function readyItems(f){try{return C()?.findNextBatch?.({exam:f.exam,subject:f.subject,topic:f.topic,visualPreferred:true},5)||[]}catch{return[]}}
  function reason(f){if((f.total||0)<3)return`${f.topic} için ölçüm az. Önce gerçek kaynak sorularıyla seviyeyi netleştirip sonra kısa tekrar yapacağız.`;if((f.score??100)<60)return`${f.topic} son ölçümlerde öncelik istiyor. Bugünkü plan: ölç → eksik noktayı tekrar et → yanlışları kapat.`;if((f.recentSignals||0)>0)return`${f.topic} için yakın zamanda zorlanma sinyali var. Uzun ders yerine kısa ve kontrollü bir çalışma yeterli.`;return`${f.topic} bugün kısa pekiştirme için uygun görünüyor.`}
  function recent(f){return D()?.recentForTopic?.(f.topic,5)||[]}
  function topicTestInsight(f){return(D()?.getTopicTestInsights?.({days:60})||[]).find(x=>x.topicId===f.topicKey||(x.exam===f.exam&&norm(x.subject)===norm(f.subject)&&norm(x.topic)===norm(f.topic)))||null}
  function decorateTopicTestEvidence(f){
    const insight=topicTestInsight(f);if(!insight)return;
    const status=document.querySelector('#teacher .pt2-main .pt2-card:last-child');
    if(status){const note=document.createElement('div');note.className='pt2-note';note.innerHTML=`<b>Konu testi kanıtı:</b> ${insight.totalTests} test • ${insight.totalQuestions} soru • ${insight.correct}D ${insight.wrong}Y ${insight.blank}B • ${insight.net.toLocaleString('tr-TR')} net • %${insight.accuracy} • ${esc(insight.trend)}.<br>Toplu performans sinyalidir; kesin ustalık veya soru bazlı yanlış değildir.`;status.appendChild(note)}
    const details=document.querySelectorAll('#teacher .pt2-details details'),perf=details[1]?.querySelector('.pt2-perfs');
    if(perf)perf.innerHTML=insight.recentTests.map(x=>`<span class="pt2-perf">${x.questionCount} soru • ${x.correct}D ${x.wrong}Y ${x.blank}B • %${x.accuracy} • ${esc(x.dateKey)}</span>`).join('');
  }

  function taskHtml(n,title,desc,done,active,button,label,disabled=false){const cls=done?'done':active?'active':'locked',status=done?'Tamamlandı':active?'Sıradaki':'Bekliyor';return `<div class="pt2-task ${cls}"><div class="pt2-num">${done?'✓':n}</div><div><h4>${esc(title)}</h4><p>${esc(desc)}</p></div><div><span class="pt2-status">${status}</span>${button?`<button id="${button}" class="${active?'primary':'ghost'}" ${disabled?'disabled':''} style="margin-left:8px">${esc(label)}</button>`:''}</div></div>`}

  function render(){if(window.__teacherPolicyPending){root.innerHTML='<div class="screen-head"><button class="back" data-go="home">←</button><h1>Kişisel Öğretmenim</h1></div><div class="pt2-card"><b>Kişisel Öğretmen hazırlanıyor…</b><p class="muted">Öğrenci verileri ve gerçek kaynak havuzu kontrol ediliyor.</p></div>';root.querySelector('[data-go="home"]')?.addEventListener('click',()=>go('home'));return}const f=focus(),d=ensureDaily(f);syncWrong(d);const ready=readyItems(f),perf=recent(f),done=[d.testDone,d.recapDone,d.wrongDone].filter(Boolean).length,mistakes=Number(d.testSummary?.mistakes||0),step1=!d.testDone,step2=d.testDone&&!d.recapDone,step3=d.testDone&&d.recapDone&&!d.wrongDone;root.innerHTML=`<div class="screen-head"><button class="back" data-go="home">←</button><h1>Kişisel Öğretmenim</h1></div><div class="pt2"><div class="pt2-hero"><div><div class="pt2-chips"><span class="pt2-chip">${esc(`${f.exam} ${f.subject}`)}</span><span class="pt2-chip green">Bugünün konusu</span></div><h2>${esc(f.topic)}</h2><p>${esc(reason(f))}</p></div><div class="pt2-progress"><b>${done}/3</b><small>bugünkü görev</small><div class="pt2-bar"><i style="width:${Math.round(done/3*100)}%"></i></div></div></div><div class="pt2-main"><div class="pt2-card"><div class="pt2-head"><h3>Bugünkü 3 Adım</h3><small>Bir adım bitmeden sonraki açılmaz</small></div><div class="pt2-tasks">${taskHtml(1,'Kaynakla Seviye Ölçümü',ready.length?`${ready.length} hazır, çözülmemiş gerçek kaynak sorusuyla ölçüm.`:'Bu konu için henüz hazır kaynak sorusu bulunamadı.',d.testDone,step1,'pt2Test',d.testDone?'Tekrar Ölç':ready.length?`${ready.length} Soruluk Ölçümü Başlat`:'Soru Hazırlanıyor',!ready.length)}${taskHtml(2,'Sonuca Göre Hızlı Tekrar','AI yalnız konuyu açıklar; yeni soru üretmez.',d.recapDone,step2,'pt2Recap',d.recapDone?'Tekrar Aç':'Hızlı Tekrarı Aç',!d.testDone)}${taskHtml(3,'Yanlışları Düzelt',d.testDone&&d.testSummary?(mistakes?`${mistakes} yanlış/yapamadım kaydı varsa tek-soru ekranından tekrar incele.`:'Ölçümde yanlış yok; bu adım otomatik tamamlandı.'):'Ölçüm bittikten sonra gerekli yanlışlar burada kapanır.',d.wrongDone,step3,'pt2Wrong',d.wrongDone?'Yanlışları Aç':'Yanlışlarımı Aç',!d.testDone||!d.recapDone||!mistakes)}</div><div id="pt2RecapSlot" style="margin-top:12px"></div></div><div class="pt2-card"><div class="pt2-head"><h3>Kısa Durum</h3><small>Bugün</small></div><div class="pt2-metrics"><div class="pt2-metric"><b>${f.score==null?'—':`%${f.score}`}</b><small>ölçümlü başarı</small></div><div class="pt2-metric"><b>${f.wrong||0}</b><small>ölçümlü yanlış</small></div><div class="pt2-metric"><b>${ready.length}</b><small>hazır kaynak soru</small></div><div class="pt2-metric"><b>${d.testSummary?`%${d.testSummary.percent}`:'—'}</b><small>bugünkü ölçüm</small></div></div><div class="pt2-note">Görev ancak gerçekten tamamlandığında ✓ olur. Testi yalnız açmak tamamlanmış sayılmaz.</div></div></div><div class="pt2-details"><details><summary>Öğretmenin konu seçimini değiştir</summary><div class="pt2-topics">${candidates().slice(0,5).map(x=>`<button class="pt2-topic ${norm(x.topic)===norm(f.topic)?'active':''}" data-pt2-topic="${esc(x.topic)}"><span><b>${esc(x.topic)}</b><br><small class="muted">${esc(`${x.exam} ${x.subject}`)}</small></span><span>${x.score==null?'—':`%${x.score}`}</span></button>`).join('')}</div></details><details><summary>Son performansı göster</summary><div class="pt2-perfs">${perf.length?perf.map(x=>`<span class="pt2-perf ${x.result==='correct'?'ok':'bad'}">${x.result==='correct'?'✓':'✕'} ${new Date(x.timestamp||Date.now()).toLocaleDateString('tr-TR',{day:'2-digit',month:'2-digit'})}</span>`).join(''):'<span class="muted">Henüz ölçüm yok.</span>'}</div></details></div></div>`;
    decorateTopicTestEvidence(f);root.querySelector('[data-go="home"]').onclick=()=>go('home');root.querySelectorAll('[data-pt2-topic]').forEach(b=>b.onclick=()=>{state.teacher.selectedTopic=b.dataset.pt2Topic;state.strategy??={};state.strategy.manualTopicDate=today();state.strategy.manualTopic=b.dataset.pt2Topic;state.teacher.daily=null;save();render()});
    const t=root.querySelector('#pt2Test');if(t)t.onclick=()=>launchTest(f);const r=root.querySelector('#pt2Recap');if(r)r.onclick=()=>openRecap(f);const w=root.querySelector('#pt2Wrong');if(w&&!w.disabled)w.onclick=()=>openWrongs();
  }

  function selectOption(el,value){const n=norm(value),o=[...el.options].find(x=>norm(x.value)===n||norm(x.textContent)===n)||[...el.options].find(x=>norm(x.value).includes(n)||n.includes(norm(x.value)));if(!o)return false;if(el.value!==o.value){el.value=o.value;el.dispatchEvent(new Event('change',{bubbles:true}));return true}return false}
  function launchTest(f){const d=ensureDaily(f);state.miniTests.teacherTask={date:today(),topic:f.topic,exam:f.exam,subject:f.subject};state.miniTests.prefillSubject=`${f.exam} ${f.subject}`;state.miniTests.prefillTopic=f.topic;d.testLaunchedAt=Date.now();save();go('tests');window.renderMiniTestHome?.();let tries=0;const setup=()=>{if(++tries>40)return;const ex=document.getElementById('mtsExam');if(!ex){setTimeout(setup,70);return}if(selectOption(ex,f.exam)){setTimeout(setup,70);return}const sub=document.getElementById('mtsSubject');if(!sub){setTimeout(setup,70);return}if(selectOption(sub,f.subject)){setTimeout(setup,70);return}const top=document.getElementById('mtsTopic');if(!top){setTimeout(setup,70);return}selectOption(top,f.topic);const count=document.getElementById('mtsCount');if(count)count.value='5';const build=document.getElementById('mtsBuild');if(build)setTimeout(()=>build.click(),50);else setTimeout(setup,70)};setTimeout(setup,100)}

  async function openRecap(f){if(!ensureDaily(f).testDone)return;const slot=document.getElementById('pt2RecapSlot');if(!slot)return;let data=state.teacher.recapCache?.[f.topic];if(!data){slot.innerHTML='<div class="pt2-recap"><b>✍️ Hızlı tekrar hazırlanıyor…</b></div>';try{const r=await fetch('/api/teacher-recap',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({exam:f.exam,subject:f.subject,topic:f.topic,mastery:f.score??0,recentWrongCount:f.recentWrong||f.wrong||0})}),j=await r.json();if(!r.ok)throw new Error(j.error||'Tekrar hazırlanamadı');state.teacher.recapCache[f.topic]=j;save();data=j}catch(e){slot.innerHTML=`<div class="pt2-recap"><b>Tekrar hazırlanamadı</b><p>${esc(e.message)}</p></div>`;return}}slot.innerHTML=`<div class="pt2-recap"><h3>${esc(data.title||f.topic)}</h3><p class="muted">${esc(data.overview||'')}</p><div class="pt2-key">${(data.key_points||[]).map((x,i)=>`<div><b>${i+1}. nokta</b><br>${esc(x)}</div>`).join('')}</div><div class="pt2-warn"><b>⚠️ Sık hata</b><br>${esc(data.common_mistake||'')}</div><div class="pt2-tip"><b>💡 Sınav püf noktası</b><br>${esc(data.exam_tip||'')}</div><div class="pt2-recap-actions"><button id="pt2RecapDone" class="primary">✓ Tekrarı Tamamladım</button><button id="pt2RecapClose" class="ghost">Kapat</button></div></div>`;slot.querySelector('#pt2RecapDone').onclick=()=>{const d=ensureDaily(f);d.recapDone=true;save();render()};slot.querySelector('#pt2RecapClose').onclick=()=>slot.innerHTML=''}

  function openWrongs(){const d=state.teacher.daily;if(!d)return;state.teacher.returnFromWrong=true;d.wrongReviewBaseline??=reviewsToday();save();go('wrong');setTimeout(decorateWrongReturn,80)}
  function decorateWrongReturn(){if(!state.teacher.returnFromWrong)return;const wr=document.getElementById('wrong');if(!wr)return;let b=wr.querySelector('.teacher-return');if(!b){b=document.createElement('div');b.className='teacher-return';b.innerHTML='<span><b>🧠 Kişisel Öğretmen görevi</b><br><small class="muted">Yanlışı inceledikten sonra öğretmene dön.</small></span><button class="primary">Öğretmene Dön</button>';const head=wr.querySelector('.screen-head');head?.insertAdjacentElement('afterend',b);b.querySelector('button').onclick=()=>{const d=state.teacher.daily;if(d&&reviewsToday()>Number(d.wrongReviewBaseline||0))d.wrongDone=true;state.teacher.returnFromWrong=false;save();go('teacher')}}}

  document.addEventListener('click',e=>{if(e.target.closest('#mtsFinish')&&state.miniTests?.teacherTask){const ctx={...state.miniTests.teacherTask};setTimeout(()=>{const h=(state.miniTests?.history||[])[0];let f=focus(),d=ensureDaily(f);if(ctx.date===today()&&norm(ctx.topic)===norm(d.topic)){d.testDone=true;const count=Number(h?.count||0),correct=Number(h?.correct||0);d.testSummary={count,correct,percent:Number(h?.percent||0),mistakes:Math.max(0,count-correct),finishedAt:Date.now()};d.wrongReviewBaseline=reviewsToday();if(d.testSummary.mistakes===0)d.wrongDone=true;save()}state.miniTests.teacherTask=null;save();go('teacher');setTimeout(()=>window.renderTeacher?.(),30)},40)}if(state.teacher.returnFromWrong&&e.target.closest('#wrong2Host [data-wrong2]'))setTimeout(()=>{const d=state.teacher.daily;if(d&&reviewsToday()>Number(d.wrongReviewBaseline||0)){d.wrongDone=true;save()}decorateWrongReturn()},100)},true);

  const oldGo=window.go;if(typeof oldGo==='function'&&!oldGo.__teacherV2){const wrapped=function(id){const r=oldGo(id);if(id==='teacher')setTimeout(render,0);if(id==='wrong')setTimeout(decorateWrongReturn,60);return r};wrapped.__teacherV2=true;window.go=wrapped;try{go=wrapped}catch{}}
  window.renderTeacher=render;try{renderTeacher=render}catch{};if(root.classList.contains('active'))render();
})();
